import { ApplyBtnDirective } from './apply-btn.directive';

describe('ApplyBtnDirective', () => {
  it('should create an instance', () => {
    const directive = new ApplyBtnDirective();
    expect(directive).toBeTruthy();
  });
});
