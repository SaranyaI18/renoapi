import { ApplicationformClassTs } from './applicationform-class.ts';

describe('ApplicationformClassTs', () => {
  it('should create an instance', () => {
    expect(new ApplicationformClassTs()).toBeTruthy();
  });
});
